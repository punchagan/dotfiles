############################################
# Configuration for mobile-git-sync script #
############################################
#
# Sourced from bash script, no spaces
# around the assignment operators.

# Path to git repo used for synchronization.
WORK_REPO="$HOME/.life-in-plain-text/src"

# Git branch to merge changes into.
MERGE_BRANCH="master"

# Git branch used to track changes on the mobile device.
MOBILE_BRANCH="mobile"

# Space separated list of files in repo to synchronize.
SYNC_FILES="*.org"

# Commit message to use for all commits.
COMMIT_MESSAGE="Changes from mobile at $(date)"

# If non-blank, pull MERGE_BRANCH from this remote before synchronizing, and push
# it when done.
GIT_REMOTE="origin"

# URL to directory on mobile deveice. All protocols supported by curl can be used.
# Examples:
# MOBILE_URL="ftp://mymobile.example.com:3000/files"
# MOBILE_URL="scp://192.168.0.12/files
MOBILE_URL="scp://192.168.0.103:2222/sdcard/sshd/life-in-plain-text"
