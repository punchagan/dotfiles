#!/bin/bash

DAY="${1:-last Friday}"
opam switch okra
eval "$(opam env)"
WEEK=$(date -d "${DAY}" '+%U')
YEAR=$(date -d "${DAY}" '+%G')
DIR="${HOME}/code/segfault/admin/weekly/${YEAR}/${WEEK}"
mkdir -p "${DIR}"
okra generate --week="${WEEK}" > "${DIR}/$(whoami).md"
