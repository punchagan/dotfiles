# Optional argument for number of days
if [ -z $1 ]
then
days=1
else
days=$1
fi

branches=`git branch|tr \* " "|tr -d " "`

for branch in $branches
do
output=`git log --since="$days days ago" $branch`
if [ ! -z "$output" ]
then
echo $branch
git log -n 1 --oneline $branch
echo
fi
done